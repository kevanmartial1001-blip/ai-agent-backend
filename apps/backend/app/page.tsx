export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <h1>AI Agent Backend</h1>
      <p>APIs: <code>/api/chat</code>, <code>/api/heygen-token</code>, <code>/api/stt</code>, <code>/api/heygen-sdk</code></p>
      <p>UI: <code>/call</code></p>
    </main>
  );
}
